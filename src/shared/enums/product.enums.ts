export enum PriceType {
  STANDARD = 'STANDARD',
  DISCOUNTED = 'DISCOUNTED',
  SALE = 'SALE',
}

export enum ProductStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum FileType {
  PDF = 'PDF',
  EXCEL = 'EXCEL',
}
